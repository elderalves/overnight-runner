.alves/
.conductor/
node_modules/

# Per-repo overnight-runner artifacts (this repo doubles as a target repo for its own smoke tests).
# jobs/ and runs/ stay untracked everywhere the runner points, per jobs-runs-git-tracking.md.
jobs/
runs/
.worktrees/